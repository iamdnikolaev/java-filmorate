merge into ratings (rating_id, name)
    values(1, 'G');

merge into ratings (rating_id, name)
    values(2, 'PG');

merge into ratings (rating_id, name)
    values(3, 'PG-13');

merge into ratings (rating_id, name)
    values(4, 'R');

merge into ratings (rating_id, name)
    values(5, 'NC-17');

merge into genres (genre_id, name)
    values(1, 'Комедия');

merge into genres (genre_id, name)
    values(2, 'Драма');

merge into genres (genre_id, name)
    values(3, 'Мультфильм');

merge into genres (genre_id, name)
    values(4, 'Триллер');

merge into genres (genre_id, name)
    values(5, 'Документальный');

merge into genres (genre_id, name)
    values(6, 'Боевик');

merge into genres (genre_id, name)
    values(7, 'Научная фантастика');

merge into genres (genre_id, name)
    values(8, 'Биография');

merge into genres (genre_id, name)
    values(9, 'Ужасы');

merge into genres (genre_id, name)
    values(10, 'Эротика');
